name: flutter_scout_helper
description: Flutter helper bridge that gives Flutter Scout VM-service eyes and hands inside debug apps.
version: 0.2.0-dev.1
repository: https://github.com/khorlim/flutter_scout

environment:
  sdk: ^3.12.2
  flutter: ">=3.44.2"

dependencies:
  crypto: ^3.0.7
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
